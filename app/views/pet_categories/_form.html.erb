<%= form_for(@pet_category) do |f| %>
  <% if @pet_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_category.errors.count, "error") %> prohibited this pet_category from being saved:</h2>

      <ul>
      <% @pet_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
